"""The experimentation harness: tools, protocols, runs.

Nothing here is imported by ``amide.cli`` at module level; commands import
what they need when they run so that ``amide --help`` stays fast.
"""
